import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const FilteredDetailsScreen = ({}) => {
  return (
    <View style={styles.container}>
      <Text>Hello</Text>
    </View>
  );
};

export default FilteredDetailsScreen;

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});
